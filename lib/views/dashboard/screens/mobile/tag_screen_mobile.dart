import 'package:flutter/material.dart';
class TagScreenMobile extends StatelessWidget {
  const TagScreenMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
