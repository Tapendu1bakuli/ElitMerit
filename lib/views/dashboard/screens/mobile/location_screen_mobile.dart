import 'package:flutter/material.dart';
class LocationScreenMobile extends StatelessWidget {
  const LocationScreenMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
