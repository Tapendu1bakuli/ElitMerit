import 'package:flutter/material.dart';
class WhatsAppScreenMobile extends StatelessWidget {
  const WhatsAppScreenMobile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
