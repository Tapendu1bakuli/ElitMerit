import 'package:flutter/material.dart';
class SearchScreenTablet extends StatelessWidget {
  const SearchScreenTablet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
