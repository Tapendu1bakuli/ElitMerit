import 'package:flutter/material.dart';
class WhatsAppScreenTablet extends StatelessWidget {
  const WhatsAppScreenTablet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
