import 'package:flutter/material.dart';
class TagScreenTablet extends StatelessWidget {
  const TagScreenTablet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
