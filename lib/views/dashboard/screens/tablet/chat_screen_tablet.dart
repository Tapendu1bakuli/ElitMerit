import 'package:flutter/material.dart';
class ChatScreenTablet extends StatelessWidget {
  const ChatScreenTablet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
