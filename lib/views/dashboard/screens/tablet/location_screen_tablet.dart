import 'package:flutter/material.dart';
class LocationScreenTablet extends StatelessWidget {
  const LocationScreenTablet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
